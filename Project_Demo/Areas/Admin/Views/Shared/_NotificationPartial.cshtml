@if(TempData["succes"] != null){
    <div class="alert alert-succes notification">
        @TempData["succes"]
    </div>
}

@if(TempData["error"] != null){
    <div class="alert alert-danger notification">
        @TempData["error"]
    </div>
}